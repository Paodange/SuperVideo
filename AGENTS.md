# SuperVideo 智能体开发规范

本文件适用于在本仓库工作的所有开发智能体。

## 分支与集成流程

1. `main` 是唯一集成分支。除仓库治理文档得到用户明确许可外，不得直接在 `main` 上开发功能。
2. 开始任务前，确认工作区干净，从最新本地 `main` 创建新分支。分支名使用 `<type>/<task-id>-<short-name>`，例如 `feat/a01-monorepo-scaffold`。
3. 一个分支只完成一个工作包，不夹带重构或其他功能。发现无关问题时记录在交接说明中，不顺手扩大范围。
4. 开发智能体必须在任务分支上完成实现、测试、文档和提交，不得自行合并 `main`，不得自行推送远程仓库，除非用户明确改变授权。
5. 完成时向主智能体提供：分支名、提交哈希、变更摘要、实际执行的验证命令及结果、已知限制、需要人工关注的风险。
6. 主智能体负责本地集成到 `main` 并做轻量 Review。Review 至少检查任务范围、关键 diff、测试结果、配置/密钥泄漏、生成文件和向后兼容性。
7. Review 未通过时不得推送 `main`；修复应形成清晰提交并重新验证。Review 通过后，主智能体可以将 `main` 推送到 `origin`。
8. 禁止对 `main` 强制推送，禁止使用破坏性 reset 覆盖其他人的工作。确认远程推送成功前保留任务分支。

## 开发要求

- 以 `docs/phase-1-technical-design.md` 为当前产品和架构基线；若实现需要改变边界，先提出并记录设计变更。
- 每个 2–3 天工作包都必须产生可运行、可演示的结果，并满足技术方案中的统一完成定义。
- Windows 是第一阶段目标平台。脚本和命令必须在 PowerShell 环境可用，路径处理不得假设 POSIX。
- 原始视频素材只读引用；测试不得删除或覆盖项目外部素材。
- API Key、访问令牌和用户隐私数据不得进入代码、Prompt 样例、日志、测试快照或提交历史。
- 新增依赖需要说明用途，锁文件必须提交；不要提交 `node_modules`、虚拟环境、缓存、构建产物和临时会话。
- 对外契约、数据库和 Timeline IR 必须版本化。影响契约的改动需要迁移或兼容说明。
- 优先运行与改动直接相关的测试；交接前至少运行仓库约定的静态检查和健康检查。

## 提交建议

- 使用清晰的小提交，提交信息推荐 `<type>(<scope>): <summary>`。
- 常用类型：`feat`、`fix`、`refactor`、`test`、`docs`、`build`、`chore`。
- 不要重写已共享的提交历史。
